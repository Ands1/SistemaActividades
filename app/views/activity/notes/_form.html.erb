<%= form_with(model: activity_note) do |form| %>
  <% if activity_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_note.errors.count, "error") %> prohibited this activity_note from being saved:</h2>

      <ul>
        <% activity_note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
